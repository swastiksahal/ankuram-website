# W2.2 — /how-we-teach — 21 September 2026

**Build verified. STAGING DEPLOYMENT BLOCKED. No production write.**

Repository: `swastiksahal/ankuram-website`, branch `ankuramtuition-com`.
Starting commit: `876cfd595bd6b4e380c8655e65cc40563fb2cbf5`.
Read `CLAUDE.md` including the Wave roadmap, `docs/APPROVED-CHANGES.md`,
the design system, staging setup, and W2.1 deployment record before building.

## Route confirmed before building

| Live request | Observed response |
|---|---|
| `/how-we-teach` | 200, no redirect |
| `/how-we-teach/` | 301 → `https://ankuramtuition.com/how-we-teach` |
| `/how-we-teach.html` | 301 → `https://ankuramtuition.com/how-we-teach` |

The repository's current `public_html/.htaccess` removes `.html` and trailing
slashes, then internally rewrites the extensionless path to the existing flat
file `public_html/how-we-teach.html`. The fetched live HTML equals that source
file byte for byte (SHA-256 prefix `d3d2b66e`). Origin filesystem inspection was
unavailable because SSH could not connect; the route evidence is the current
repository plus the observed live HTTP responses.

The new output is **`build/staging/how-we-teach.html`**, with no
`how-we-teach/` directory. Assets are `css/site.css?v=ef951be3` and
`js/contact-whatsapp.js?v=5f7ccd03`, relative to the containing web root. These
also resolve inside `/staging/` when using the second staging entrance.

## Reuse and CSS

- `design/wave2/blocks.js` is byte-identical to the starting commit. Header,
  hero, FAQ, and footer are called directly. No block variant or fork.
- Body layouts alternate: split introduction, session timeline, comparison,
  batch panel, method rows, signs cards, online/plans split, parent-question
  cards, FAQ, bottom CTA. No added explanatory copy or diagram labels.
- CSS: **43 insertions, 0 deletions**. Original 63,566 bytes remain an exact
  prefix; 3,221 bytes appended; new size 66,787 bytes. Every added selector
  starts with `.v2.w2` and also requires a new `w2-teach-*` class. None matches
  the homepage or diagnostic page.
- A23 hashes are derived and checked by the builder/assembler, never typed into
  the generated HTML. CSS `ef951be3`; unchanged JS `5f7ccd03`.
- New page tracking uses the three approved A1/A19 scripts lifted from the
  current homepage. No IDs were copied from legacy page tracking.

## Head parity — byte-identical fields

| Field | /how-we-teach | Homepage | /diagnostic-assessment |
|---|---|---|---|
| Title | PASS | PASS | PASS |
| Meta description | PASS | PASS | PASS |
| Canonical | PASS | PASS | PASS |
| Meta robots | PASS | PASS | PASS |
| Hreflang / absence | PASS | PASS | PASS |
| Every OG tag | PASS — 9 | PASS — 9 | PASS — 9 |
| Every Twitter tag | PASS — 4 | PASS — 4 | PASS — 4 |
| JSON-LD blocks, bytes and order | PASS — 2 | PASS — 5 | PASS — 3 |
| H1 | PASS | PASS | PASS |

For `/how-we-teach`, JSON-LD block 1 and block 2 each match byte for byte.
Per-tag head output is in `verification/*-head-parity.txt`.

Staging HTML preserves live meta robots exactly, as requested. Index exclusion
therefore depends on the **existing HTTP `X-Robots-Tag` and basic auth** on both
staging entrances; these were observed and remain unchanged. Nothing here is a
proposal to expose staging publicly.

`head-parity.js --head-only` runs the head/heading gates without its
homepage-specific tracking/script-order assumptions. Runtime inline functions
are tested separately with the original `check-inline-handlers.mjs`. This does
not claim a fresh full tracking audit of the existing diagnostic page: its
current live HTML has no gtag or Clarity loader and was preserved unchanged.

## Content and links

| Gate | Result |
|---|---|
| Live H2s retained as H2 | **10/10; 0 missing** |
| Live H3s retained as H3 | **28/28; 0 missing** |
| FAQ questions | **7/7**, `<summary><h3 class="faq-question">…</h3></summary>` |
| Visible word count | **2,036 / 2,037 = 99.95%** |
| Distinct baseline internal hrefs | **14/14** |
| Baseline internal anchor occurrences | **27/27** |
| Exact internal anchor text/href pairs | **27/27 occurrences retained** |
| Main content wording | **Exact ordered text match**, excluding the A6 sticky controls |
| New page copy | **None** |

Word accounting: shared chrome adds `Skip to content` (3), `ANKURAM` (1),
`Menu` (1), and header `WhatsApp` (1). It removes one repeated navigation phone
number (3 tokens) and A6 sticky `Call Now` (2) / `WhatsApp` (1): net zero.
The one-word difference from the historical 2,037 fingerprint is an entity
counting artifact: that decoder counts literal `&rarr;` as a word; the existing
Wave 2 extractor correctly emits its visible arrow `→`, which is not a word.
The visible main text is unchanged; long paragraphs are split only at existing
sentence boundaries. No prices, diagnostic durations, claims, or IB wording
were corrected or expanded.

All internal destinations retained verbatim:

| href | Baseline occurrences retained |
|---|---:|
| `/` | 1 |
| `/#home` | 2 |
| `/#about` | 3 |
| `/#contact` | 3 |
| `/#curricula` | 3 |
| `/how-we-teach` | 3 |
| `/online-maths-tuition` | 3 |
| `/online-class-10-tuition` | 1 |
| `/diagnostic-assessment` | 3 |
| `/cbse-tuition-jubilee-hills` | 1 |
| `/icse-tuition-hyderabad` | 1 |
| `/igcse-tuition-jubilee-hills` | 1 |
| `/ib-dp-tuition-hyderabad` | 1 |
| `/a-level-tuition-hyderabad` | 1 |

## Existing pages — zero regression

Current live homepage, diagnostic page, and CSS were fetched before building;
all matched the corresponding repository files exactly. Full-page screenshots
were then taken from the actual live URLs and compared with the local staging
candidate in the same Chromium 153 engine, DPR 1, 900px viewport height.
Outbound analytics scripts and the external Maps iframe were neutralised
identically for stable comparison and to avoid sending test analytics.

| Page | Width | Changed pixels |
|---|---:|---:|
| Homepage | 390 | **0** |
| Homepage | 1440 | **0** |
| /diagnostic-assessment | 390 | **0** |
| /diagnostic-assessment | 1440 | **0** |

Each live/candidate pair also has identical PNG SHA-256, recorded in
`verification/w22/results.json`. Lossless WebP copies of the captures are in
`verification/w22/screenshots/`; their decoded RGBA pixels were checked against
the original PNGs. One image per identical pair is retained, plus both new-page
screenshots.

Homepage and diagnostic candidate HTML differ from current live HTML only in
the derived CSS hash. Their production build files and the four shared blocks
were not changed. `parity.js` and head parity were rerun on both: **PASS**.
Diagnostic parity retains 11/11 internal destinations and all 23 href
occurrences. Its pre-existing footer Home anchor-label difference (`/` versus
`/#home`, with `/` still on the brand link) is logged, not introduced here.

Visual review caught a class reused on both three section wrappers and their
inner grids, narrowing desktop columns. Corrected in new-page markup only;
shared assets and both regression pages remained byte-identical. New-page
render/Lighthouse checks were rerun after that correction.

## Browser verification

| Width | Overflow, FAQ closed | Overflow, FAQ open | Console errors |
|---:|---:|---:|---:|
| 360 | 0 | 0 | 0 |
| 390 | 0 | 0 | 0 |
| 768 | 0 | 0 | 0 |
| 1024 | 0 | 0 | 0 |
| 1280 | 0 | 0 | 0 |
| 1440 | 0 | 0 | 0 |

FAQ opens and closes with JavaScript disabled: **PASS**.

| Lighthouse 12.2.1, local candidate | Mobile | Desktop |
|---|---:|---:|
| Performance | **99** | **100** |
| Accessibility | **100** | **100** |
| SEO | **100** | **100** |
| CLS | **0** | **0** |

These are local candidate measurements with external trackers blocked, not
post-deployment measurements or a claim about field performance. No historical
Lighthouse sample for this URL exists in `baseline/lighthouse-summary.json`.

Original `design/check-inline-handlers.mjs`, real browser:

- Homepage: all **4** referenced functions resolve; **no page errors**; PASS.
- Diagnostic: `trackPhoneClick` resolves; **no page errors**; PASS.
- How-we-teach: `trackPhoneClick` resolves; **no page errors**; PASS.

## Deployment — blocked before any write

Ran `bash scripts/deploy-staging.sh build/staging`. It stopped at step 1,
the read-only production hash capture:

```text
== 1. live config fingerprint BEFORE
ssh: connect to host 145.79.212.4 port 65002: Network is unreachable
```

Exit status 255. No backup/upload/configuration command was reached.
**No staging write and no production write occurred.** All three assembled
pages render locally; authenticated rendering of the new build on the remote
staging host is **NOT VERIFIED**, because it was not deployed.

Observed current protection, before the failed deployment attempt:

| Existing staging entrance | Status | Header |
|---|---|---|
| `https://staging.ankuramtuition.com/` | **401** | `X-Robots-Tag: noindex, nofollow, noarchive, nosnippet` |
| `https://ankuramtuition.com/staging/` | **401** | `X-Robots-Tag: noindex, nofollow, noarchive, nosnippet` |

Both also return `WWW-Authenticate: Basic realm="Ankuram staging"`.
These are existing-host checks, **not** evidence of a successful deployment.

## Resume on a host with the existing SSH access

Run the committed full verification command, then the existing staging-only
deploy script. No production deployment is authorised by this report.

```bash
node design/wave2/extract.js public_html/how-we-teach.html design/wave2/how-we-teach.json
node design/wave2/build-how-we-teach.js
node scripts/make-w22-staging.js
node scripts/parity.js build/staging/how-we-teach.html /how-we-teach
node scripts/parity.js build/staging/diagnostic-assessment.html /diagnostic-assessment
node scripts/parity.js build/staging/index.html
node design/wave2/verify-how-we-teach.mjs
bash scripts/deploy-staging.sh build/staging
```

After upload, verify all three authenticated staging pages, their served asset
hashes, both unauthenticated 401 entrances, and unchanged production hashes.
Until then W2.2 remains **built and verified locally; not deployed**.
